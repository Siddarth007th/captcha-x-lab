from ml.master.router.router import MasterRouter
import os
router = MasterRouter()
file_path = "apps/web/public/reasoning_vqa_challenge.png" 
# actually wait, where is the image located?
# let's try finding the image in the repo first
